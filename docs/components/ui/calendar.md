# Calendar

Wrapper visual sobre `react-day-picker` para selección de fechas.

## Uso básico

```tsx
import { Calendar } from "@/components/ui/calendar"

<Calendar mode="single" selected={date} onSelect={setDate} />
```

## Cuándo usarlo

- Selectores de fecha
- Agendas
- Rango temporal en filtros
