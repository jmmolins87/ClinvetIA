# Label

Etiqueta de formulario para inputs, switches o selects.

## Uso básico

```tsx
import { Label } from "@/components/ui/label"

<Label htmlFor="email">Email</Label>
```

## Buenas prácticas

- Conecta siempre con `htmlFor`
- Úsalo junto a `Input`, `Select` o `Switch`
